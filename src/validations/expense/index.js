import { expenseBody } from './expenseBody.js';
import { expenseHeaders } from './expenseHeaders.js';
import { expenseParams } from './expenseParams.js';

export const Expense = {
  expenseBody,
  expenseHeaders,
  expenseParams,
};
